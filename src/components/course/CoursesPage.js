import React, { PropTypes } from 'react'

const CoursesPage = React.createClass({
  render () {
    return (
      <div>
         <h1>Courses</h1>
      </div>

    );
  }
})

export default CoursesPage;
