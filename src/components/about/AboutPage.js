import React, { PropTypes } from 'react'

const AboutPage = React.createClass({
  render () {
    return (
        <div>
              <h1>About  </h1>
              <p>This uses React Redux and Router</p>
        </div>
    );
  }
})

export default AboutPage;
